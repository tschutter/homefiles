# git config file

[alias]
    hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
    stat = status
    unstage = reset
    visual = !gitk

[core]
    editor = mg

[github]
    user = tschutter
    token = $GITHUB_TOKEN

[user]
    name = Tom Schutter
    email = t.schutter@comcast.net
